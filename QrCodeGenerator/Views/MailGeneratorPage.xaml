<Page
    x:Class="QrCodeGenerator.Views.MailGeneratorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:QrCodeGenerator.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModel:MailGeneratorPageViewModel />
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock
            x:Uid="MailAddress"
            Grid.Row="0"
            Grid.Column="0"
            Margin="5" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="5"
            Text="{Binding Receiver, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <TextBlock
            x:Uid="Subject"
            Grid.Row="1"
            Grid.Column="0"
            Margin="5" />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <TextBlock
            x:Uid="Body"
            Grid.Row="2"
            Grid.Column="0"
            Margin="5" />
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Height="200"
            Margin="5"
            AcceptsReturn="True"
            Text="{Binding Message, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </Grid>
</Page>
