<Page
    x:Class="QrCodeGenerator.Views.PhoneNumberGeneratorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:QrCodeGenerator.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModel:PhoneNumberGeneratorPageViewModel />
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock
            x:Uid="PhoneNumberRequired"
            Grid.Row="0"
            Grid.Column="0"
            Margin="5"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Margin="5"
            Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <CheckBox
            x:Uid="ShowPhoneNumberOnQrCode"
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            IsChecked="{Binding ShowTextOnImage, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <CheckBox
            x:Uid="FormatPassword"
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            IsChecked="{Binding UseGroups, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
            Visibility="{Binding IsUseGroupsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
        <TextBlock
            x:Uid="InGroupsOf"
            Grid.Row="1"
            Grid.Column="2"
            Margin="5"
            Visibility="{Binding IsUseGroupsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
        <NumberBox
            Grid.Row="1"
            Grid.Column="3"
            Margin="5"
            KeyDown="UIElement_OnKeyDown"
            SpinButtonPlacementMode="Compact"
            Visibility="{Binding IsUseGroupsVisible, Converter={StaticResource BoolToVisibilityConverter}}"
            Value="{Binding GroupLength, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </Grid>
</Page>
